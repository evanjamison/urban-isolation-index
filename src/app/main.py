import streamlit as st
st.set_page_config(page_title='Urban Isolation Index', layout='wide')
st.title('Urban Social Isolation & Aging — Tokyo, then US Comparison')
st.write('Use the left scripts to generate data, then view comparison here.')
